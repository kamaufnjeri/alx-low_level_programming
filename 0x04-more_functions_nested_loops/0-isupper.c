#include "main.h"

/**
 * _isupper - check for uppercase character
 * @c: character to be checked
 * Return: 1 or c or 0
 */
int _isupper(int c)
{
char upper[26];

upper[26] = {case 'A' ... 'Z':};
if (c == upper)
{
	return (1);
}
else
{
	return (0);
}
